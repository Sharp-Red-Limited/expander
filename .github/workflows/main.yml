name: Run Pester Tests
on: [push]
jobs:
    test_run:
        name: Test run
        runs-on: ubuntu-latest
        steps:
            - name: checkout
              uses: actions/checkout@v1
            - name: test module
              id: test_module
              shell: pwsh
              run: |
                  ./runtests.ps1
